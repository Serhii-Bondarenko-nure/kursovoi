Думаю, сделаю две подпапки для получения данных.
В первой будут данные с API.
Во второй с сохраненного мной JSON файла.
И в основном буде получения с локального хранилища.

Также, в основном будет проверка пустое ли хранилище, если да, получает данные с API или резерва.
Эти данные записывает в локальное хранилище.
Сделаю сразу и с API и резерва.
Но в самом репозитории нужно будет сделать переменную и во всех методах проверку.
Это нужно, чтобы сразу получать данные только с резервных json файлов, а не APi.
В API ограниченное количество запроов, лучше пока работать с резервом и найти, как открыть json.

Потом же можно как-то переделать, типо проверка на сеть интернет.
Если вдруг будут нужны данные, но их нет в локальном хранилище.
Тогда еще нужно будет скачать всю 1323 гифки)))))
